# 다음에 할 일

> 2026-07-29 03:11 ~ 03:13 에 정리. **1·2·4·5·6번은 끝났다.**
> 남은 것은 3번(도면 수정, 사용자가 직접)과 아래 '이것 말고 남아 있던 것' 이다.
>
> ⚠ **2·5번은 앱스크립트를 다시 배포해야 동작한다** —
> `배포 → 배포 관리 → 연필 → 버전: 새 버전` (저장만 하면 반영 안 된다)

## 1. 홀 블룸 줄이기 ✅

홀을 30cm 로 눕히면서 농도를 `0.06 → 0.2` 로 올렸는데
(`src/materials.js` 의 `TYPE_STYLE.hall`), 넓은 면이 블룸을 그대로 먹어 눈이 아프다.
2층 홀 767 m², 3층 홀 654 m² 라 면적이 커서 더 그렇다.

손댈 곳 후보 — 어느 쪽이 나은지 눈으로 비교할 것:

- `TYPE_STYLE.hall` 의 `opacity` 를 0.2 → 0.12 정도로
- 블룸 임계값(`postfx.js` 의 `BLOOM`)을 올려 밝은 면만 번지게
- 홀 색을 지금보다 어둡게 (슬레이트 `#94a3b8` → 조금 낮춘 값)

## 2. 승인을 취소·반려로 되돌리면? ✅

지금은 선생님이 시트에서 `승인` → `취소`/`반려` 로 바꾸는 경우를 안 따져봤다.

- 조회 응답을 60초 캐시하므로 화면에 바로 안 비친다
- **이미 "승인됐다" 고 안 신청자에게는 아무 통보가 없다**
- 그 시간이 다시 비는 것 자체는 `상태` 만 보므로 자동으로 맞는다

→ 상태가 바뀔 때 신청자에게 메일을 보내는 `onEdit` 트리거를 검토.

## 3. 도면 수정 — 내가 직접 할 것 ⚠

**`꿈세움 라운지 바테이블` → `꿈세움 라운지`** (3동 4층)

Sweet Home 3D 에서 라벨을 고친 뒤 `py -3 tools/parse_sh3d.py` 만 돌리면 된다.

주의: 같은 이름이 **3층 3동에도 하나 더 있다.** 둘 다 바꿀지 4층만 바꿀지 정할 것.
4층만 바꾸면 3층은 `꿈세움 라운지 바테이블` 로 남는다.

> 코드나 데이터를 직접 고치지 않는다. 도면이 원천이다.

## 4. 계단실·엘리베이터를 통짜 기둥으로 ✅

지금은 층마다 따로 만든 칸을 이어 붙여 기둥처럼 보이게 한 것이라, **층을 하나만
켜면 그 층 칸만 남는다.** 층이 잘려도 1층부터 4층까지 관통하는 기둥이 그대로
박혀 있게 바꾼다.

만들 때 걸릴 점 — 층별 메시와 별개로 기둥 하나를 더 만들고 층 격리 상태에 따라
둘 중 하나만 보이게 하는 방식이 될 것이다. 반사(`createReflection`)와 필터
(`applyFilters`)에도 같이 반영해야 한다.

## 5. 현재 예약 현황 보기 ✅

지금은 방을 하나씩 열어야 그 방 예약이 보인다. 전체를 한 번에 보는 화면이 필요하다.

서버 쪽은 이미 있다 — `GET ?action=day&date=…` 가 그 날 모든 방의 승인 현황을
`{ sourceId: [{start, end}] }` 로 준다. 화면만 만들면 된다.

- 날짜를 고르면 그 날 예약된 방을 지도에서 다른 색으로 (명세서 8절에 적어둔 것)
- 또는 목록으로: 방 / 시간 / 신청자 / 사유

## 6. 학급 규칙 써두기 ✅

방마다 쓰는 규칙(예: 사용 후 정리 필수, 반입 금지 물품)을 어딘가 적어둘지 검토.
`plan.config.json` 의 `reservable` 에 방마다 `note` 를 넣을 자리를 이미 만들어 뒀다
(명세서 4절). 예약 창에서 신청 전에 보여주면 된다.

---

## 이것 말고 남아 있던 것

- **실제 예약 신청(POST) 한 번 확인** — 시트에 줄이 생기고, 메일이 오고,
  `승인` 으로 바꾸면 그 교시가 잠기는지. 읽기는 확인했지만 쓰기는 아직이다.
- 예약 열 방 13개는 임시다
- 담당 교사·취소 정책·신청 가능 기간은 기본값 상태
  ([예약 시스템 명세](예약-시스템-명세.md) 의 '남은 결정')
- 글꼴 2MB → woff2 로 바꾸면 700KB 정도

---

## 07-29 낮에 한 것 (1·2·4·5·6)

- **1. 홀 블룸** — 농도 0.2 → 0.12, 색도 한 단 낮췄다(`lighten: -0.1`).
  눕히면서 농도를 올린 게 원인이었다. 면이 넓을수록 같은 밝기라도 눈에 들어오는
  빛의 총량이 커진다는 걸 놓쳤다.
- **2. 상태 변경 통보** — `onStatusEdit` 트리거를 추가했다. 승인만이 아니라
  **되돌리는 경우가 더 중요하다** — "승인됐다" 고 알고 있는 사람에게 취소·반려를
  안 알리면 그 시간에 그냥 온다. 캐시도 같이 버려서 화면에 바로 비친다.
  **연락처가 이메일일 때만 보낸다** (아래 참고).
- **4. 통짜 기둥** — 계단실·엘리베이터를 층 그룹에서 빼내 `roomGroup` 에 직접 달고
  층 필터를 건너뛰게 했다. 카메라 맞추기(`isShown`)에는 안 쓴다 — 쓰면 4층만 봐도
  기둥 전체가 들어오려고 뒤로 물러난다. 이름표는 지금 층 것만 남긴다.
- **5. 예약 현황** — 왼쪽 패널에 `예약 현황 보기`. 날짜를 고르면 그 날 승인된 예약을
  시간순으로 펼친다. 서버의 `action=day` 에 신청자·사유·방이름을 추가했다.
- **6. 이용 규칙** — `plan.config.json` 의 `reservable` 안에 `note` 를 적으면 예약 창
  맨 위에 뜬다. 제1과학실·무용실에 예시를 넣어뒀다.

## 문자(SMS) 통보는 왜 안 넣었나

앱스크립트는 문자를 직접 못 보낸다. 외부 API(알리고·쿨SMS 등)를 붙여야 하는데
**유료**(건당 10~20원)에 **발신번호 사전등록이 법적 의무**라 학교 명의 등록이
필요하다. 카카오 알림톡은 비즈니스 채널 심사까지 붙는다.

그래서 **연락처가 이메일이면 자동으로 보내고, 전화번호면 선생님이 직접 연락**하는
쪽으로 뒀다. 나중에 학교 차원에서 발신번호를 등록할 수 있으면 `onStatusEdit` 의
메일 보내는 자리에 SMS API 호출을 하나 더 넣으면 된다.
